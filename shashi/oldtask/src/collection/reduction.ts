import {Reducee } from "./reduce.js";

export class Reduction{
    score:Reducee[];
    constructor(score:Reducee[]){
        this.score=score
    }
    method():number:Reducee
    {
     const scoresum=m.reduce(function(total:number,curVAl:number): number)
     {
        return total+curVal
        
     }
       }
    }